# Task-6-sales_Trend
I created the online_sales dataset, inserted sample records, group data by year and month using EXTRACT, Calculated revenue using COUNT(DISTINCT order_id), and sorted results using order by.
